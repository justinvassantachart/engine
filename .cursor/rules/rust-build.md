In this project, to verify that the Rust code compiles, run `npm run build`.
Although this is counterintuitive, the npm build script orchestrates the Rust build (e.g., via wasm-pack/cargo or other tooling). Always use `npm run build` when checking whether the Rust project compiles.
